server:
  port: 9095

spring:
  application:
    name: gateway
    # URL of config server for fetching the configuration
  config:
    import: "optional:configserver:http://localhost:8071" # URL of your Config Server
#    gateway configuration.
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: product-service-route
              uri: lb://PRODUCTSSERVICE
              predicates:
                - Path=/product, /product/**

            - id: coupon-service-route
              uri: lb://COUPONSERVICE
              predicates:
                - Path=/coupon, /coupon/**
#Eureka client configuration.
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # the url of the service registry server.
  instance:
    hostname: localhost